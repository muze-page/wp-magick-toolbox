# 禁止生成缩略图

::: warning 谨慎
关闭后主题和插件可能无法获取合适尺寸的图片。
:::

## 功能说明

WordPress 上传图片时会自动生成多个尺寸的缩略图。此功能关闭自动生成的多尺寸图片，节省存储空间。

## 使用场景

- 存储空间有限：虚拟主机磁盘空间较小
- 使用 CDN/OSS：图片存储在云端，本地不需要多份副本
- 主题自行处理图片尺寸

## 开启方法

1. 进入 **魔法工具箱** → **媒体优化**
2. 开启「禁止生成缩略图」
3. 保存配置

## 注意事项

开启前确认当前主题不依赖 WordPress 自动生成的缩略图尺寸。
