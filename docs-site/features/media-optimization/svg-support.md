# SVG 图标支持

::: tip 推荐
安全且建议开启。
:::

## 功能说明

WordPress 默认不允许上传 SVG 文件（出于安全考虑）。此功能启用 SVG 上传和预览支持。

## 使用场景

- 图标系统：使用 SVG 作为站点图标和页面图标
- 矢量图形：需要无损缩放的图形资源
- 性能优化：SVG 文件通常比 PNG 更小

## 开启方法

1. 进入 **魔法工具箱** → **媒体优化**
2. 开启「SVG 图标支持」
3. 保存配置

## 注意事项

仅允许上传可信来源的 SVG 文件，恶意 SVG 可能包含 XSS 攻击代码。
