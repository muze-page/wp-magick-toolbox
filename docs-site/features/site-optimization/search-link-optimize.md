# 搜索链接优化

::: tip 推荐
安全且建议开启。优化搜索 URL 格式。
:::

## 功能说明

WordPress 默认搜索 URL 格式为 `?s=关键词`。此功能将其优化为 `/search/关键词`，使搜索 URL 更友好。

## 使用场景

- SEO 优化：更友好的 URL 结构有利于搜索引擎收录
- 用户体验：URL 更直观，易于分享

## 开启方法

1. 进入 **魔法工具箱** → **站点优化**
2. 开启「搜索链接优化」
3. 保存配置

## 注意事项

开启后旧格式 URL 仍可访问，会自动重定向到新格式。
